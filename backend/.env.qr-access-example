# ===================================
# QR ACCESS SYSTEM CONFIGURATION
# ===================================
# Add these lines to your existing .env file

# Internal API Key for Desktop App authentication
# IMPORTANT: Change this to a secure random key in production
INTERNAL_API_KEY=azpoolarena-internal-qr-2026

# Frontend URL for QR code generation
# Used to build the QR URL: {FRONTEND_URL}/attendance/check-in?token=xxx
FRONTEND_URL=http://localhost:5173

# ===================================
# PRODUCTION NOTES
# ===================================
# 1. Generate a secure API key:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 2. Update FRONTEND_URL to your production domain:
#    FRONTEND_URL=https://yourdomain.com
#
# 3. Ensure Desktop App .env has matching INTERNAL_API_KEY
